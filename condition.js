// conditional expressions
            /*  [extra :-]
            promt() is used by browser to get input from user
            alert() is used by browser to display output to user
            parseInt() is used to convert string to integer
            */
            let sign = prompt("What's your sign?");
 if (a>=18) {
    alert("you are a minor");
 }
 console.log(a);